---
description: Scraper allowlist, no link-following, site parser shape
globs: **/scraper/**/*.py
alwaysApply: false
---

# Scraper

## Allowlist

- Support only an explicit, small allowlist of sites.
- Route via `dispatch.py` (site → parser lookup). Do not scrape arbitrary hosts.

## Fetch / parse rules

- Scrape **only** the given URL. Never follow links found on the page.
- Use `httpx` (async) + BeautifulSoup with `lxml`.

## Site parsers (`scraper/sites/`)

- One module per site.
- Expected shape: HTML → job-fields dict matching `jobs` sheet columns (`job_title`, `job_description`, `job_location`, `job_company`, `job_salary`, `job_type`, `job_posted_date`, etc.).
- Each module exports `SITE_HOST` (hostname, lowercase, no `www.`) and `parse`; `sites/__init__.py` auto-registers them into `SITE_ALLOWLIST`. No manual dispatch edit.
